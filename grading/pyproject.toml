[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ml4t-grading-utils"
version = "0.1.0"
description = "Shared grading and validation utility code for ML4T assignments."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []
